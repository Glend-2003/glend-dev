# Origen del sitio en produccion. Se resuelve en tiempo de BUILD, asi que tiene
# que existir al compilar: es lo que hace que hreflang, la URL canonica y las
# Open Graph apunten al dominio real en vez de a localhost.
NEXT_PUBLIC_SITE_URL=https://glend-dev.glendrojas1040.workers.dev
